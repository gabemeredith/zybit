/**
 * Colors used by every rule's `proposeAnnotations` overlay and by the
 * legend rendered in `AnnotatedFindingPreview`. Kept in one file so a
 * future redesign edits one place — the rules and the legend stay in
 * sync without manual copy-paste.
 */
export const ANNOTATION_HEAVY_COLOR = '#ef4444';   // red — "your design emphasizes this / problem element"
export const ANNOTATION_CLICKED_COLOR = '#22c55e'; // green — "your users gravitate here"
export const ANNOTATION_WARN_COLOR = '#f59e0b';    // amber — "this is where users hesitate / look for help / over-disperse"
